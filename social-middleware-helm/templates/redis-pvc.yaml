apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: netapp-block-standard
  resources:
    requests:
      {{- if hasSuffix "-prod" .Release.Namespace }}
      storage: {{ .Values.redis.prod.storage.size }}
      {{- else }}
      storage: {{ .Values.redis.dev.storage.size }}
      {{- end }}